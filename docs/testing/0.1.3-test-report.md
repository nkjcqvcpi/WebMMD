# WebMMD 0.1.3 Validation & Test Report

## Git Release Info

- **Version:** `0.1.3`
- **License Compliance:** Verified standard SPDX AGPL-3.0-or-later headers are declared across all source files, metadata, and crates.

## Core Test Suite

- **Vitest Unit Tests:**
  - `packages/protocol/tests/protocol.test.ts` (1 test passed)
  - `packages/viewer/tests/path.test.ts` (19 tests passed)
  - **Path Resolution Coverage:** 100% Statement/Function/Branch/Line coverage.
- **Cargo Rust Tests:**
  - `test_group_flip_morph_graph` (passed)
  - `test_append_grants` (passed)
  - `test_material_morph` (passed)
  - `test_math` (passed)
  - `test_ik_solvers` (passed)
  - `test_parse_malformed_signature` (passed)
  - `test_parse_truncated_data` (passed)
  - `test_quat_euler_xyz_conversions` (passed)
  - `test_validation_errors` (passed)
  - `test_bone_morph` (passed)
  - `test_parse_lynette` (passed)
  - **Status:** All 11 tests passed.

## Safari Web Automation Validation

- **Driver:** System Safari WebDriver (Selenium).
- **Execution Log Output:**
  - Initial load validation (Logo "WebMMD", version verified).
  - ZIP upload and extraction via background worker (`zip.worker.ts`).
  - Discovered and listed PMX options in zip.
  - Model selection, background parsing (`parser.worker.ts`), and WebGPU resources upload.
  - Model load verification (Inspector model name: "Linette").
  - Morph activation verification (Activated "まばたき" and "笑い" at weight 0.8).
  - **Rendering Verification:** Verified screenshot size difference (>50KB) and correct rendering checks to guard against blank canvases.
  - **Repeated-Load Leak Verification:** Reloaded model 3 times and checked that frame counter increases.
- **Status:** PASSED.
